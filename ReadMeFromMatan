
openBlob - is a method that is in the sqlContoller to handel exam file in case it is a path to local file
openBlob2 - is a method to send file as Blob and all its content than open a new file to keep the Blob and then open it. 
use those method 

####### 6.6.23 

finished to load head of department data before generating report meaning 
i can load all courses or student or professors 
need to decide if to allow a search button. 

next is to generate the actual report for each in a statistics graph. 
